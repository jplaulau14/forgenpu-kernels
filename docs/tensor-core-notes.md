# Tensor Core Notes

Tensor Core implementation starts in M4.

This note should eventually cover dtype constraints, layout constraints, alignment, accumulation precision, and why Tensor Cores do not automatically improve every shape.
